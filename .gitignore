# Dependencies
node_modules/
app/node_modules/

# Build outputs
app/dist/
app/.vite/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Env
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*

# Heavy raw data — re-downloadable from https://geoportal.provita.org.ve/
raw-sources/
data/provita/
GEOjson files/
*.zip

# INE raw sources — re-descargables con `node scripts/fetch-ine-population.mjs`
data/ine-raw/

# Documento de referencia que dejé en la carpeta (no parte del proyecto)
/Anexo_Municipios*.html
/Anexo_Municipios*_files/

# Big GeoJSON kept only locally (deforestación 388MB etc.)
*.tif
*.tiff

# Excels y CSVs sueltos en la raíz (los oficiales viven en data/*)
/*.xlsx
/*.csv

# Documentos privados de trabajo (no publicar)
/revision.html

# Descargas raw para vista global (Natural Earth + PDFs R4V, regenerables)
data/diaspora-raw/

# Reportes intermedios de cosecha Wiki (unmatched munis, etc.). Regenerable
# con `node scripts/fetch-wiki-info.mjs`. La salida final útil
# (app/src/data/wiki-info.json + flags/shields) sí se commitea.
data/wiki-raw/
